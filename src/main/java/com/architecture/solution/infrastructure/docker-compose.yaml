services:
  my-sql:
    image: mysql:8.4
    container_name: mysql-db
    volumes:
      - mysql_data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: hello
    ports:
      - "3306:3306"
volumes: 
  mysql_data:


   