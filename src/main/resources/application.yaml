server:
  port: 8080

spring:
  application:
    name: architecture-solution

  datasource:
    url: jdbc:mysql://localhost:3306/products
    username: root
    password: rootpassword

  jpa:
    hibernate:
      ddl-auto: update